// `pnpm capture-auth` — opens Playwright in headed mode against a target site,
// lets the operator log in manually, then writes storage state to auth-state.json.
//
// Currently a stub. Implemented in §12 step 3.

console.log('capture-auth CLI: not yet implemented (§12 step 3).');
process.exit(1);
