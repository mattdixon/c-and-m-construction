{
  "name": "@aggregate/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not yet wired'",
    "capture-auth": "tsx src/cli/capture-auth.ts"
  },
  "dependencies": {
    "@aggregate/db": "workspace:*",
    "@anthropic-ai/sdk": "^0.32.1",
    "playwright": "^1.49.0",
    "postgres": "^3.4.5",
    "resend": "^4.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
